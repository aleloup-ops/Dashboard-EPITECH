version: '3'

networks:
  communication: {}
services:
  server:
    restart: always
    build:
      context: server
    volumes:
      - ./server:/code
    command: python manage.py runserver 0.0.0.0:8080
    ports:
      - "8080:8080"
    networks:
      -  communication
  frontend:
    build:
      context: front/app
    volumes:
      - ./front/app:/app
    ports:
      - "4200:4200"
    networks:
      - communication
volumes:
  myapp: